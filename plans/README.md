# Implementation Plans

Generated by the improve skill on 2026-07-02. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                   | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Bring TypeScript and lint guidance under CI-safe checks | P1       | M      | -          | DONE   |
| 002  | Redact secret-bearing values from errors                | P1       | S      | -          | TODO   |
| 003  | Confine imported skill slugs to `skills/imported`       | P1       | S/M    | -          | TODO   |
| 004  | Preflight merged cleanup locally and close docs drift   | P2       | M      | -          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency notes

- Plans 001, 002, and 003 are independent. They can run in any order.
- Plan 004 is also independent, but it touches the active cleanup feature and should be
  reviewed by someone familiar with `mt cleanup --merged`.
- If only one plan can run first, start with 001 because it strengthens the verification
  baseline for later changes.

## Findings covered

- Plan 001 covers: CI does not run TypeScript checking, `tsconfig.json` only includes
  `src/**/*.ts`, and README/AGENTS lint guidance points at the mutating `bun run lint`
  script instead of the check-only command.
- Plan 002 covers: error messages can echo secret-bearing env, DSN, URL, and Resource
  value contents.
- Plan 003 covers: imported-skill recipe slugs are not path-confined before copy/remove
  operations.
- Plan 004 covers: `cleanup --merged` performs GitHub/default-branch lookup before local
  skip checks, and cleanup docs/backlog state are stale relative to shipped behavior.

## Findings considered and rejected

- Local branch deletion after merged cleanup: rejected as a finding because
  `docs/adr/0003-remove-only-worktrees-for-merge-cleanable-session-cleanup.md` explicitly
  keeps branch deletion out of scope for the first version.
- Shell directory directive for `mt spawn` and `mt swing`: rejected as a finding because
  `docs/adr/0004-use-shell-integration-for-spawn-and-swing-navigation.md` records this as
  the intended parent-shell navigation mechanism.
- Resource command stdout omission on thrown/nonzero failures: rejected as a finding because
  `__tests__/resource-commands.integration.test.ts` asserts stdout is omitted from thrown
  resource command failures to avoid leaking secret stdout.
- Honoring repo-authored `bootstrapCommand`, `cleanupCommand`, and Resource command modules:
  rejected as a finding because `CONTEXT.md` and README describe these as repo-owned local
  automation hooks; the hardening work is to avoid leaking values and to validate paths, not
  to remove the feature.
