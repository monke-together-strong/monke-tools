Distributed package updates must include a matching Tegami release entry under `.tegami/` that
describes the consumer-visible change and appropriate version impact.
Put the release-note body under a Markdown heading (e.g. `## Fix configuration`) after the YAML
frontmatter.
